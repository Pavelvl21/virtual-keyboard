import ru from './ru';
import en from './en';
import servicesKeys from './servicesKeys';

const { services, digits, shifted } = servicesKeys;

const locales = {
  en,
  ru,
  services,
  digits,
  shifted,
};

export default locales;
